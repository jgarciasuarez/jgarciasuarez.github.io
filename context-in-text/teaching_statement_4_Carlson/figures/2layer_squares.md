# 2layer_squares.pdf Description

This image is a mathematical diagram showing the addition of two 2D scalar fields over a square domain defined by `[0, 2\pi] \times [0, 2\pi]`. 

- The leftmost square shows a field denoted as `\zeta_2` with diagonal stripes of blue, red, and grey.
- The middle square is added to it (`+` sign), showing a field denoted as `\zeta_1` with similar diagonal stripes but oriented differently.
- The rightmost square shows the resulting sum (`=` sign), which forms a complex, alternating red, blue, and grey checkerboard-like interference pattern.
- A colorbar at the bottom ranges from -1.0 to 1.0, mapping values to colors (red for negative values around -1.0, grey for values around 0, and blue for positive values around 1.0), representing the variable `T_k`.
